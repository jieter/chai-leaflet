{
  "commitMessage": "Bumped version to %s",
  "tagName": "%s",
  "scripts": {
      "preupdate": "npm run test && npm run test-node",
      "postupdate": "git push && git push --tags && npm publish"
  }
}
